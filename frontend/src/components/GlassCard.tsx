import { ReactNode } from 'react'; import { StyleProp, StyleSheet, View, ViewStyle } from 'react-native'; import { colors, shadows, spacing } from 'src/theme'; interface GlassCardProps { children: ReactNode; style?: StyleProp<ViewStyle>; } export default function GlassCard({ children, style }: GlassCardProps) { return <View style={[styles.card, style]}>{children}</View>; } const styles = StyleSheet.create({ card: { backgroundColor: colors.surfaceGlass, borderRadius: 24, borderWidth: 1, borderColor: 'rgba(255,255,255,0.55)', padding: spacing.lg, overflow: 'hidden', ...shadows.soft, }, });